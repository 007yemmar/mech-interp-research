# Python
__pycache__/
*.py[cod]
*$py.class
.Python

# Virtual env + tooling
.venv/
.python-version-*

# IDE / OS
.DS_Store
.idea/
.vscode/
.ipynb_checkpoints/

# Secrets / local env
.env
.env.*

# Data — never commit PHI-adjacent files
data/
outputs/
.tmp/
*.csv
*.parquet
*.pt
*.safetensors
*.npy
*.npz

# results/ — only aggregate summary JSONs are tracked; row-level CSV / npz /
# per-note artifacts pulled from Modal stay local (see results/README.md).

# Overleaf paper working copy — local-only. Manuscript sources, figures and
# build artifacts are synced through Overleaf, not this repo. Contents are
# ignored wholesale, so nothing drafted there can be committed by accident
# (it will contain figures derived from MIMIC-IV — see the PHI rules in
# CLAUDE.md and results/README.md).
paper_overleaf/

# Modal local state
.modal/

# Superpowers workspace (specs, plans) — personal working artifacts
docs/superpowers/*
!docs/superpowers/specs/
docs/superpowers/specs/*
!docs/superpowers/specs/2026-05-23-clinical-sae-defense-guide.md
