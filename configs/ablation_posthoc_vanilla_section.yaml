# Post-hoc analyses on the VANILLA SECTION-LOCAL run (#5, /out/ablation/vanilla_section).
#
# Emits the standard #2/#3/#4 blocks (window-based; identical to the original
# vanilla zero-ablation since same intervention/notes) PLUS a "section_local"
# block: mean section vs rest Cliff's delta and the concentration (section-rest)
# across the 30 grounded features — the #5 headline.
#
# Run (CPU, ~seconds):
#   modal run modal_app/ablation_posthoc.py --config-file configs/ablation_posthoc_vanilla_section.yaml

ablation_output_dir: /out/ablation/vanilla_section

icd_csv_path: /data/sample_50k.csv
activations_dir: /out/activations/google-gemma-2-2b_L16_50000notes_39c5801_20260423T193837Z_centered

join_key: admission_id
icd_col_prefix: "icd9_"
held_out_shard_start: 281
held_out_shard_end: 312
shard_checkpoint_subdir: shard_results
output_subdir: posthoc_specificity

restrict_true_negative: true
min_off_target_pos: 10

logging_level: INFO
