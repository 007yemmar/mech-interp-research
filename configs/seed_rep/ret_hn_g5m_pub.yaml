# JumpReLU s2 concordance replication -- ret_hn_g5m, pub pool
#
# PRELIMINARY: SAE = ${jumprelu_run} @ step_00046000 (no LR decay, no final/).
# Protocol = vanilla's: pool selection, r values, contexts and retrieval slates all
# come from the 31 held-out shards (281-311) -- ${jumprelu_eval} covers exactly
# those. Every value other than the paths is copied verbatim from
# configs/judge3/ret_hn_van_pub.yaml, so these rows sit next to the published §14 rows.
# Generated 2026-09-26 (spec: docs/superpowers/specs/2026-09-26-jumprelu-s2-concordance-design.md).
#
# uv run modal run --detach modal_app/retrieval_eval.py --config-file configs/seed_rep/ret_hn_g5m_pub.yaml --detach

auto_interp_dir: ${jumprelu_auto_interp}
icd_eval_dir: ${jumprelu_eval}
icd_csv_path: /data/sample_50k.csv
icd_keywords_yaml_path: /root/configs/icd9_keywords_improved.yaml
output_dir: ${jumprelu_auto_interp}/retrieval_eval_hardneg
judges:
- {slug: gpt-5-mini, backend: openrouter, model: openai/gpt-5-mini, reasoning_effort: minimal}
same_chapter_distractors: true
n_distractors: 7
r_unrelated: 0.05
thresholds: [0.3, 0.5]
max_workers: 4
client_max_retries: 8
logging_level: INFO
