# Fourth judge (Google Gemini 2.5 Flash) via OpenRouter — de-anchored concordance (r sentence removed), jr.
# Explanations held fixed; only the judge changes. Extends RESULTS.md §14.7 to four labs.
auto_interp_dir: ${jumprelu_s0_auto_interp}
output_dir: ${jumprelu_s0_auto_interp}/deanchored_eval
judges:
  - {slug: gemini-2.5-flash, backend: openrouter, model: google/gemini-2.5-flash}
client_max_retries: 8
logging_level: INFO
