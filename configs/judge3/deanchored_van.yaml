# Third judge (OpenAI GPT-5-mini) via OpenRouter — de-anchored concordance (r sentence removed), van.
# Explanations held fixed; only the judge changes. Extends RESULTS.md §14.7 to three labs.
auto_interp_dir: /out/auto_interp/vanilla_test_split
output_dir: /out/auto_interp/vanilla_test_split/deanchored_eval
judges:
  - {slug: gpt-5-mini, backend: openrouter, model: openai/gpt-5-mini, reasoning_effort: minimal}
client_max_retries: 8
logging_level: INFO
